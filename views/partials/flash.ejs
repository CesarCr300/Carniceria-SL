<% if (exito && exito.length) {%>
    <div class="alert alert-success" role="alert">
        <%= exito %>
    </div>
    <% } %>
        <% if (error && error.length) {%>
            <div class="alert alert-danger" role="alert">
                <%= error %>
            </div>
            <% } %>